export const signup = (_req, res) => {
  res.json({ message: "API đăng ký hoạt động" });
};

export const login = (_req, res) => {
  res.json({ message: "API đăng nhập hoạt động" });
};

export const logout = (_req, res) => {
  res.json({ message: "API đăng xuất hoạt động" });
};
